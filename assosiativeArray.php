<?php 

    // $foods = [

    //     'vegetables' => 'brinjal','onion','brookly','etc',
    //     'fruits ' => 'banana','apple','orange',
    //     'drinks' => 'milk','water'

    // ];

    // $foods['drinks'] = $foods['drinks'].",orange juice"; // If access to any key and value by this way..It can be add value in a key

    // foreach($foods as $key=>$value){  //foreach loop is the best way to iterate values from the key

        // echo "$key"."="."$value"."\n";
    // }

    //another way to find assosiatve key and value by array_keys and values..
    //remember print_r(), this function is used in array print

    // $keys = array_keys($foods); // this is the array_keys() 
    // print_r($keys);
    // for($i=0;$i<count($keys);$i++){
    //     $key = $keys[$i];
    //     echo $foods[$key]."\n";
    //  }

    //  $values = array_values($foods); //This is the array_values() 
    //  print_r($values);
    //  for($i=0;$i<count($values);$i++){
    //      $value = $values[$i];
    //      echo $value."\n";
    //   }

//End of the print_r() function


    //---Now this is the array which works with String to array, Array to String//Multiple Diamiters---

    
        // $vegitables = explode(', ','brinjal,onion,brookly,etc');

        
        // // var_dump($vegitables);

        // echo $vegitables[0];

        
    

